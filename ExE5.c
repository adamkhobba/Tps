









#include <stdio.h>

int main ()
{
// 1)

	size_t	i;
	size_t	n;

	printf("taper le number de dimension de tableau");
	scanf("%d", n);
	printf("remplir le tableau mais si tu veut passe a une dimension a une autre taper espace ' ' ^ @_@");
	i = 0;
	while (i < n)
	{
		j = 0;
		while (1)
		{
			printf("Dimeension %d", i);
			scanf ("%d",T[i][j]);
			if ( )
				break ;
		}
		i++;
	}
}
